<!--<Window x:Class="LoginWindow.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LoginWindow"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Button Content="Cancel" HorizontalAlignment="Left" Margin="441,222,0,0" VerticalAlignment="Top" Height="56" Width="115" Click="OnClick"/>
        <Button Content="Add" HorizontalAlignment="Left" Margin="247,217,0,0" VerticalAlignment="Top" Height="65" Width="115" Click="OnClickAdd"/>
        <TextBox x:Name="Nav" HorizontalAlignment="Left" Margin="200,122,0,0" TextWrapping="Wrap" Text="UserName" VerticalAlignment="Top" Width="152" Height="31"/>
        <TextBox  x:Name="Van" HorizontalAlignment="Left" Margin="400,122,0,0" TextWrapping="Wrap" Text="Password" VerticalAlignment="Top" Width="152" Height="31"/>

        <Grid>
            <Image Width="Auto" Height="Auto"
HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
Stretch="UniformToFill"
Source="car.jpg" />
        </Grid>
    </Grid>
    

</Window>-->
<Window x:Class="LoginWindow.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LoginWindow"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <!-- Define Rows and Columns -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!-- For the Image -->
            <RowDefinition Height="Auto" />
            <!-- For the User Input Fields -->
            <RowDefinition Height="Auto" />
            <!-- For the Buttons -->
            <RowDefinition Height="*" />
            <!-- Empty Space (Optional) -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <!-- Left Column -->
            <ColumnDefinition Width="*" />
            <!-- Right Column -->
        </Grid.ColumnDefinitions>

        <!-- Image -->
        <Image Grid.Row="3" Grid.ColumnSpan="2" 
               Stretch="Uniform" 
               Source="car.jpg" 
               Margin="10,40,10,38" />

        <!-- Username TextBox -->
        <TextBox x:Name="Nav" 
                 Grid.Row="1" Grid.Column="0" 
                 Text="UserName" 
                 Margin="10" 
                 VerticalAlignment="Center" 
                 HorizontalAlignment="Stretch" />

        <!-- Password TextBox -->
        <TextBox x:Name="Van" 
                 Grid.Row="1" Grid.Column="1" 
                 Text="Password" 
                 Margin="10" 
                 VerticalAlignment="Center" 
                 HorizontalAlignment="Stretch" />

        <!-- Add Button -->
        <Button Content="Add" 
                Grid.Row="2" Grid.Column="0" 
                Margin="10" 
                Height="40" 
                Click="OnClickAdd" Background="#FFD99999" />

        <!-- Cancel Button -->
        <Button Content="Cancel" 
                Grid.Row="2" Grid.Column="1" 
                Margin="10" 
                Height="40" 
                Click="OnClick" />
    </Grid>
</Window>
